<?php

namespace ConferenceDomain\Models\Concerns;

use Carbon\Carbon;

trait ConferenceAccessor
{
    // public function getSocialNameAttribute($value): string
    // {
    //     return html_entity_decode($value);
    // }

    // public function getStatusAtAttribute($value):? string
    // {
    //     if (is_array($value) && array_key_exists('date', $value)) {
    //         $value = $value['date'];
    //     }

    //     if (empty($value)) {
    //         return NULL;
    //     }

    //     return Carbon::parse($value)->format('d/m/Y H:i');
    // }
}
